# Budget Planner (C#)

A simple console-based budget tracker, built while learning C#. Users can add expenses, view them, and generate a category-based spending report.

**Concepts covered:** collections (`List<T>`), and LINQ (`GroupBy`, `Sum`, `Select`, `OrderByDescending`).

## Running it

```
dotnet run
```
